# BIET — Python dependencies (Phase 1: data foundation)
# Install:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt

# --- ingestion ---------------------------------------------------------------
httpx==0.28.1
pandas==3.0.5

# --- validation & config -----------------------------------------------------
pydantic==2.13.4
pydantic-settings==2.15.0

# --- persistence -------------------------------------------------------------
SQLAlchemy==2.0.52
psycopg[binary]==3.3.4
alembic==1.19.1
pgvector==0.5.0            # VECTOR column type for the guideline corpus

# --- guideline corpus (data/ingestion/publish/corpus.py) ---------------------
fastembed==0.8.0           # local embedding model (BAAI/bge-small-en-v1.5), no API key
pypdf==6.16.2               # PDF text extraction

# --- testing -----------------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.152.4        # Phase 2 — engine property tests

# --- quality gates (biet-backend skill section 9) -----------------------------
mypy==2.3.1                # --strict must pass on biet_engine
ruff==0.16.4

# --- not yet required; added at the phase that needs them --------------------
# fastapi          # Phase 3 — API
# uvicorn          # Phase 3 — API
# numpy            # Phase 2 — engine (arrives with pandas today)
# anthropic        # Phase 5 — narrative
# reportlab        # Phase 5 — PDF export
# python-pptx      # Phase 5 — PPTX export
